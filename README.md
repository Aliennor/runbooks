# Runbooks

Public runbook drop. One curated runbook at a time.

## Current

- [RUNBOOK_DEV_FULL_STATE_EXPORT_FOR_K8S_MIGRATION_BANKA_KATILIM_ZT_2026_05_15.md](RUNBOOK_DEV_FULL_STATE_EXPORT_FOR_K8S_MIGRATION_BANKA_KATILIM_ZT_2026_05_15.md) — operator-runnable export of every DEV-server state surface (Postgres, MySQL, ClickHouse, MinIO, ES/OpenSearch, OpenWebUI, n8n, secrets) into separate artifacts for a k8s migration importer. Driver script: [`scripts/k8s_full_export.sh`](scripts/k8s_full_export.sh).
- [RUNBOOK_LITELLM_LOKI_FORENSIC_AUDIT_2026_05_18.md](RUNBOOK_LITELLM_LOKI_FORENSIC_AUDIT_2026_05_18.md) — audit a deployed LiteLLM proxy's stdout in Loki for admin-API mutations and `Authorization`/`Bearer`/`sk-` header leakage over an arbitrary window. Solves the two common deployment gotchas (Loki not bound to host port, Loki's ~30-day `max_query_length` cap). Driver script: [`scripts/loki_litellm_forensic.sh`](scripts/loki_litellm_forensic.sh).
- [RUNBOOK_RAGFLOW_MINIO_EXITED_RESTART_2026_05_21.md](RUNBOOK_RAGFLOW_MINIO_EXITED_RESTART_2026_05_21.md) — bring a RagFlow MinIO container (`docker_minio_1` / `docker-minio-1`) back up after it exits and the other containers in the stack came back without it. Single-container `docker start` path so compose isn't rerun (avoids RagFlow ES `_state/` desync). Includes pre-start inspect (exit code, OOM, mounts, disk-free, port holders) and post-start readiness probe.
- [RUNBOOK_BANKA_DEV_GRAFANA_DATASOURCE_502_PROXY_FIX_2026_06_01.md](RUNBOOK_BANKA_DEV_GRAFANA_DATASOURCE_502_PROXY_FIX_2026_06_01.md) — Grafana shows no data, all datasources return 502, because the container inherits the host corporate HTTP proxy and routes `prometheus:9090` / `loki:3100` through it. Fix: add `NO_PROXY` to Grafana's environment and force-recreate. Patch image: `aliennor/banka-dev108-grafana-no-proxy:2026-06-01-r1`.
- [RUNBOOK_OBSERVABILITY_PROMETHEUS_NO_TARGETS_NETWORK_2026_06_01.md](RUNBOOK_OBSERVABILITY_PROMETHEUS_NO_TARGETS_NETWORK_2026_06_01.md) — Prometheus has no targets and all Grafana panels are blank because `internal_services_network` was destroyed when the compose project that owns it (shared-postgres) went down. Fix: bring shared-postgres up to recreate the network, then restart observability.
- [RUNBOOK_OBSERVABILITY_PROMTAIL_EXITED_GRAFANA_NO_DATA_2026_06_01.md](RUNBOOK_OBSERVABILITY_PROMTAIL_EXITED_GRAFANA_NO_DATA_2026_06_01.md) — Promtail exits with "cannot connect to Docker daemon" on a Podman host (stale `/var/run/docker.sock`), plus all Grafana panels empty after a co-located stack's compose down/up disrupts `internal_services_network`. Covers socket fix (systemctl restart or manual `podman system service`), Promtail force-recreate, Prometheus target check, full observability stack restart, and datasource provisioning reload.
- [RUNBOOK_HOST_REBOOT_RECOVERY_2026_05_23.md](RUNBOOK_HOST_REBOOT_RECOVERY_2026_05_23.md) — bring the whole internal-services Docker stack back up after a host reboot (Postgres / MySQL / ES / MinIO / Redis / RagFlow / Langfuse / LiteLLM / n8n / OpenWebUI / nginx-proxy / observability). `docker start`-only — never `compose up` — so RagFlow ES `_state/` is preserved. Driver: [`scripts/boot_recovery.sh`](scripts/boot_recovery.sh) with `dry` / quick / `tiered` modes. Includes per-stack readiness probes you can run from inside each container.
